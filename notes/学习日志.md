# 学习日志
> 记法：每天一节
> 坚持每天写 + git push

##2026-09-07
-git部分：
	（接分支冲突）
		冲突的本质是同一文件的同一区域被不同分支所修改。
		可以用git log --oneline --graph来看不同版本，git push修改origin/master
		分支管理中，git merge --no-ff -m "merge with no-ff" dev可以拒绝ff模式，也就是完整的显示分支（ff会直接一步到位合并）
		git stash可以保护现场
		强行删除分支功能：git branch -D xxx
		查看远程库信息：git remote；推送分支：git push origin（此处默认远程仓库名为origin）+分支名
		从远程库下拉：git pull = git fetch+git mege（取回云端的更新+合并）
	标签管理：
		先切换到需要打标签的分支然后git tag 标签名，可以通过git tag 查看标签。
		git log --pretty=oneline --abbrev-commit可以看到各个版本commit的id，方便补标签：git tag 标签名 commit id
		删除标签：git tag -d 标签名
-docker部分
	报错排查：daemon未启动（打开Docker Desktop等绿灯）→ 拉镜像超时（国内连不上Docker Hub）
	解决：Settings→Docker Engine 里加 registry-mirrors（国内镜像站，货一样只是取货点在国内）→ hello-world跑通
	核心概念：镜像=打包好的环境模板，容器=镜像的运行实例（一次性，--rm退出即销毁，各容器互相隔离）
	挂载：-v "本机路径:容器内路径"，容器内外共享同一文件夹（部署项目时代码/模型就这么进去）
	常用：docker run -it --rm python:3.10 bash 进容器玩；mkdir -p 是连父目录一起建
	认知：git管代码版本，conda管库版本，docker管环境搬运（"在我电脑上明明是好的"的解药）
	明日：conda env export > environment.yml（环境导出/复现）→ 阶段0收官，开学李宏毅


---

## 2026-09-06

**今日完成**：
- Ubuntu (WSL2) 安装成功，解决 0x80370102（开启虚拟机平台 + hypervisorlaunchtype——虚拟机监管平台）
- Linux 入门命令：pwd/ls/mkdir/cd/rm/cat/grep，了解 ps/kill/chmod/curl
- Git 实战：init / add / commit / push，SSH key 配置，仓库推送到 GitHub（28cs-project）
		    创建版本库：git add+git commit，commit记得要加-m"xxx"
		    回到不同版本：code xxx召唤vscode，git status能查看每次改动的状态，git diff告诉你每次改动的不同，git log观察修改日志
					      git reset --hard HEAD^可以回到HEAD的上一个版本，--hard会回退到上个版本的已提交状态，而--soft会回退到上个版本的未提交状态，--mixed会回退到上个版本已添加但未提交的状态。hard后面也可以加版本号（commit后面的一长串的前几位）回到最新版本。
		    工作区、暂存区（stage）和master：后二者合称为版本库，add是先加入stage，commit才全部移入master。对应可以理解--hard/--mixed/--soft分别回到的是哪里。
		        git restore <file>             # 后悔 1：改了没 add，想扔掉修改
			git restore --staged <file>    # 后悔 2：add 了还没 commit，想退出暂存区（改动保留）
			git reset --hard HEAD^         # 后悔 3：commit 了，想整个回退到上一个版本（危险，会丢改动）
			git checkout -- readme.txt的命令可以替代restore --staged，checkout本质是吧版本库粘贴到工作区。
		     删除文件：rm+commit提交

	远程仓库：
		SSH Key就是吧本地git连接到远程仓库
		git remote add origin git@github.com:PandaKn1ght/learning-git-practise.git
		git push -u origin master 这里-u是直接绑定，下次直接git push 就可以了
		git push origin master

	分支：
		创建&&合并：checkout -b  dev等价于 git branch dev+git checkout dev，git merge用来合并分支

---		

		


